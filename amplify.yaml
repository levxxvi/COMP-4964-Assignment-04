version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - echo "No pre-build steps"
        build:
          commands:
            - echo "Nothing to build for a static site"
        postBuild:
          commands:
            - echo "Build completed, nothing to do here"
    artifacts:
      baseDirectory: .
      files:
        - '**/*'
    cache:
      paths: []
